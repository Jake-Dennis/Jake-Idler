---
phase: 02-animation-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - apps/server/src/routes/web.ts
  - apps/server/static/css/game.css
autonomous: false
requirements:
  - ANIM-01
  - ANIM-02
  - ANIM-03
  - ANIM-04
user_setup: []

must_haves:
  truths:
    - "animateTransition uses CombatDiff to produce AnimationStep[] then passes to AnimationQueue.playSteps"
    - "All playAttackAnimation / playHitAnimation / playHealAnimation / playBlockAnimation / playDeathAnimation / playMonsterDeathAnimation delegations route through AnimationQueue"
    - "EntityAnimFSM prevents overlapping animations on the same entity"
    - "Animation timing is driven by CSS animation-duration read via getComputedStyle — no magic numbers"
    - "Original setTimeout-based animation sequencing is removed from web.ts (only tab-throttling fallback guard remains)"
  artifacts:
    - path: "apps/server/src/routes/web.ts"
      provides: "Refactored animateTransition and animation functions"
      contains: "import { CombatDiff } | AnimationQueue | EntityAnimFSM"
    - path: "apps/server/static/css/game.css"
      provides: "Animation keyframes with consistent timing, possible new animation classes"
      min_lines: 140
  key_links:
    - from: "web.ts"
      to: "apps/server/src/animation/types.ts"
      via: "imports AnimationStep, CombatEffectType"
      pattern: "import.*animation/types"
    - from: "web.ts"
      to: "apps/server/src/animation/AnimationQueue.ts"
      via: "imports AnimationQueue class and calls playSteps"
      pattern: "import.*AnimationQueue"
    - from: "web.ts"
      to: "apps/server/src/animation/EntityAnimFSM.ts"
      via: "imports EntityAnimFSM class and registers entities"
      pattern: "import.*EntityAnimFSM"
    - from: "web.ts"
      to: "apps/server/static/css/game.css"
      via: "animation class names read via getComputedStyle during queue playback"
      pattern: "getComputedStyle|animation-duration"
---

<objective>
Refactor the client-side animation functions in web.ts to use CombatDiff → AnimationQueue → EntityAnimFSM pipeline, remove embedded setTimeout-based sequencing, and sync animation timing with CSS `animation-duration` values.

Purpose: Complete the pipeline integration so all combat animations are sequenced via WAAPI promises (ANIM-01), state-driven (ANIM-02), entity-safe (ANIM-03), and CSS-timed (ANIM-04).

Output:
- Refactored `web.ts` — animation functions delegate to pipeline modules, no setTimeout chains remain
- Updated `game.css` — animation classes follow consistent timing, `animation-duration` values are the source of truth
</objective>

<execution_context>
@C:/Users/JakeP/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/JakeP/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-animation-pipeline/02-01-SUMMARY.md

Current animation functions in web.ts (lines 702-959):
- `animateTransition(prev, next)` — lines 703-772, the orchestrator that routes state diffs to specific play* functions
- `playAttackAnimation(weaponType, heroId)` — lines 775-813, handles melee/mage/range with setTimeout chains
- `createProjectile(fromEl, toEl, color, isArc)` — lines 816-875, projectile with rAF animation and trail interval + setTimeout cleanup
- `createExplosion(el, color)` — lines 877-898, 8-spark burst with setTimeout cleanup
- `playHitAnimation(heroId, damage)` — lines 901-912, flash-red + shake with setTimeout cleanup
- `playHealAnimation(heroId, amount)` — lines 914-921, pulse-green with setTimeout cleanup
- `playBlockAnimation(heroId, damage)` — lines 923-930, shield-glow with setTimeout cleanup
- `playDeathAnimation(elId)` — lines 932-937, fade-out with opacity
- `playMonsterDeathAnimation()` — lines 939-958, fade-out + particle burst with setTimeout

17 setTimeout calls in web.ts total — ~15 are animation-sequencing related, ~2 are non-animation (floatText cleanup at line 604, overlay dismiss at line 1103).

CSS animation classes currently live at game.css lines 117-134.

EntityAnimFSM lifecycle per entity: IDLE → ATTACKING → IMPACT → RECOVERY → IDLE (DEAD interrupts anywhere).
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify Phase 1 extraction before pipeline integration</name>
  <files>apps/server/src/routes/web.ts, apps/server/static/css/game.css</files>
  <action>
    Human-verify checkpoint: Confirm Phase 1 (Foundation — Asset Extraction) is complete before modifying web.ts. Also confirm 02-01 pipeline module tests pass.
  </action>
  <verify>
    <automated>npx jest --testPathPattern="(CombatDiff|AnimationQueue|EntityAnimFSM)" --no-coverage</automated>
    <human-check>See how-to-verify steps below</human-check>
  </verify>
  <done>
    Phase 1 SUMMARY exists and module tests pass. Human has confirmed Phase 1 extraction is complete.
  </done>
  <what-built>
    Before refactoring web.ts, confirm Phase 1 (Foundation — Asset Extraction) is complete. The pipeline modules (02-01) are built and verified by their test suites.
  </what-built>
  <how-to-verify>
    1. Confirm `.planning/phases/01-foundation/01-01-SUMMARY.md` exists
    2. Verify `apps/server/src/routes/web.ts` has been cleaned up by Phase 1 (CSS/HTML extracted to static files)
    3. Confirm `apps/server/static/css/game.css` exists and has animation keyframes (lines 117-134)
    4. Confirm `npx jest --testPathPattern="(CombatDiff|AnimationQueue|EntityAnimFSM)" --no-coverage` passes all 24+ tests from 02-01
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with pipeline integration, or describe any issues found</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Refactor animateTransition to use CombatDiff + AnimationQueue pipeline</name>
  <files>apps/server/src/routes/web.ts</files>
  <action>
    Refactor the client-side `<script>` block in web.ts to import and use the new animation pipeline modules.

    Since web.ts is server-rendered on Express and the animations run client-side, the animation modules must be loaded as ES module scripts or bundled. Perform the refactoring as follows:

    **A. Add ES module imports** at the top of the `<script>` block (or use dynamic import):
    
    Add these at the start of the script section (around line 511):
    ```
    import { CombatDiff, type AnimationStep } from '/static/animation/CombatDiff.js';
    import { AnimationQueue } from '/static/animation/AnimationQueue.js';
    import { EntityAnimFSM } from '/static/animation/EntityAnimFSM.js';
    ```

    **IMPORTANT:** The animation modules need to be served as static JS files to the browser. Either:
    - Option 1 (preferred): Add an `express.static()` route for `apps/server/src/animation/` → `/static/animation/` in the Express config (if not already done by Phase 1), OR
    - Option 2: If bundling is preferred, inline the module logic directly as a self-contained script block (simpler for this vanilla JS setup)

    Given the project uses vanilla HTML/CSS/JS with `express.static()`, **Option 2** (inline the pipeline logic directly in the script block) is more appropriate — the modules must run in the browser and web.ts already serves its own script block. Inline the Logic as IIFE/wrapped functions at the top of the script block, preserving the existing pattern.

    **B. Initialize pipeline globals:**
    ```
    var animFSM = new EntityAnimFSM();
    var animQueue = new AnimationQueue(animFSM);
    ```

    **C. Rewrite `animateTransition(prev, next)` (currently lines 703-772):**
    ```
    async function animateTransition(prev, next) {
      if (!next.round || !prev.round) return;
      if (next.round.round <= prev.round.round) return;

      // Register/ensure entities exist in FSM
      if (next.round.partyHeroes) {
        next.round.partyHeroes.forEach(function(h) {
          animFSM.registerEntity('hero-' + h.heroId);
        });
      }
      animFSM.registerEntity('monster-focus');

      // Compute steps via CombatDiff
      var steps = computeAnimationSteps(prev, next);

      // Queue steps — AnimationQueue handles all timing via WAAPI
      await animQueue.playSteps(steps);

      // Update HP bars after animations complete
      if (next.monsters) updateMonsterBars(next.monsters);
      if (next.round.partyHeroes) updateHeroBars(next.round.partyHeroes);

      // Add combat log entries (from step metadata)
      steps.forEach(function(s) { addCombatLogEntry(s, next); });
    }
    ```

    **D. Keep existing helper functions** that are NOT animation-sequencing:
    - `getWeaponType()` — stays (state query, not animation)
    - `floatText(x, y, text, cls)` — stays (CSS-animated DOM element, setTimeout(800ms) cleanup can stay as fallback per D-03)
    - `addLog(type, msg)` — stays (combat log)
    - `createProjectile()` — stays for now (moved to Phase 6 or kept as-is; not setTimeout-sequenced)
    - `createExplosion()` — stays for now

    **E. Replace all play*Animation functions** with calls through the pipeline:
    
    Remove the following function bodies entirely (lines 774-958):
    - `playAttackAnimation` — replaced by CombatDiff producing ATTACK steps consumed by AnimationQueue
    - `playHitAnimation` — replaced by CombatDiff producing HIT steps
    - `playHealAnimation` — replaced by CombatDiff producing HEAL steps
    - `playBlockAnimation` — replaced by CombatDiff producing BLOCK steps  
    - `playDeathAnimation` — replaced by CombatDiff producing DEATH steps
    - `playMonsterDeathAnimation` — replaced by CombatDiff producing MONSTER_DEATH steps

    **F. Add `addCombatLogEntry(step, nextState)` helper:**
    Read the combat log logic from the old animateTransition (lines 724-766) and refactor it to use the AnimationStep type to determine what log message to emit.

    Per D-01: Pure data flow — CombatDiff produces AnimationStep[], AnimationQueue consumes them.
    Per D-03: async/await replaces all callback-based setTimeout chains.
    Per D-05: FSM guards prevent overlapping animations.
  </action>
  <verify>
    <automated>cd apps/server && npx tsc --noEmit 2>&1 | Select-String -NotMatch "node_modules" | Select-String -NotMatch "TS\d{4}" ; if ($LASTEXITCODE -ne 0) { throw "TypeScript compilation errors" }</automated>
  </verify>
  <done>
    web.ts compiles without errors. AnimateTransition uses computeAnimationSteps + animQueue.playSteps. No playAttackAnimation, playHitAnimation, playHealAnimation, playBlockAnimation, playDeathAnimation, or playMonsterDeathAnimation function bodies remain. Combat log is emitted from a step-based helper.
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove embedded setTimeout animation chains and sync timing with CSS</name>
  <files>apps/server/src/routes/web.ts, apps/server/static/css/game.css</files>
  <action>
    **A. Remove animation-related setTimeout calls from web.ts.**

    Audit every `setTimeout` call in the client-side `<script>` block. Remove those that belong to the old animation-sequencing system. The surviving setTimeout calls should be:

    - `floatText` cleanup (line ~604): setTimeout(function() { el.remove(); }, 800) — keep as CSS-animated DOM element cleanup (animationend not reliable on detached elements)
    - Overlay dismiss delay (line ~1103): setTimeout for result screen auto-retry — keep (not animation-related)

    Remove these categories:
    - All setTimeout calls inside playAttackAnimation / playHitAnimation / playHealAnimation / playBlockAnimation / playDeathAnimation / playMonsterDeathAnimation — these functions are deleted in Task 2
    - setTimeout calls inside createProjectile that chain into monster flash (lines 800, 806, 810)
    - setTimeout calls inside createExplosion that do flash cleanup and spark removal (lines 892, 897)
    - Monster appearance delayed log (line 764)
    - Any setTimeout that calls classList.remove for animation cleanup patterns

    **B. Add CSS `animation-duration` properties** to game.css animation classes (currently only defined inside @keyframes, not on the classes themselves):

    Add explicit `animation-duration` to each animate-* class in game.css (around line 117-134):

    ```css
    .animate-lunge { animation: lunge .75s ease-in-out; animation-duration: .75s; z-index: 50; }
    .animate-flash-white { animation: flashWhite .35s ease; animation-duration: .35s; }
    .animate-flash-red { animation: flashRed .4s ease; animation-duration: .4s; }
    .animate-pulse-green { animation: pulseGreen .5s ease; animation-duration: .5s; }
    .animate-shake { animation: shake .3s ease; animation-duration: .3s; }
    .animate-fade-out { animation: fadeOut .6s ease forwards; animation-duration: .6s; }
    .animate-shield { animation: shieldGlow .5s ease; animation-duration: .5s; }
    .animate-explode { animation: explode .4s ease forwards; animation-duration: .4s; }
    ```

    Wait — CSS `animation` shorthand already includes duration. The duration IS in the shorthand. But `getComputedStyle(el).animationDuration` reads the resolved value. Let me verify: yes, the shorthand values ARE reflected in the computed style. So the CSS doesn't need changes — the computed style will return the correct duration from the shorthand. However, some browsers differ on whether `animation` shorthand fully populates `animationDuration`. To be explicit and avoid cross-browser issues, add `animation-duration` as a separate explicit declaration alongside the shorthand.

    **C. Wire AnimationQueue CSS duration reading:**
    
    In the inline animation pipeline code (from Task 2), ensure the AnimationQueue constructor or the css-duration resolver uses `getComputedStyle(element).animationDuration` to read the duration for each animation class. The resolver already exists in the AnimationQueue module — confirm it's correctly invoked during step playback.

    **D. Add `.animate-windup` and `.animate-fade-in` CSS classes** if needed:
    - `.animate-windup` — brief anticipation before ATTACK (if the existing lunge keyframe covers this, skip; otherwise add a 0.15s windup)
    - `.animate-fade-in` — for monster appear transition

    Per D-04: Animation durations driven by CSS `animation-duration` and propagated to JS via animationend events. No hardcoded magic numbers.
  </action>
  <verify>
    <automated>
      $scriptBlock = Get-Content apps/server/src/routes/web.ts -Raw
      # Count remaining animation-sequencing setTimeout (should be 0 for animation, only floatText + overlay survive)
      $animSetTimeoutCount = [regex]::Matches($scriptBlock, 'setTimeout\(function').Count
      Write-Host "Total setTimeout calls in web.ts: $animSetTimeoutCount (expected ≤2 non-animation)"
      if ($animSetTimeoutCount -gt 3) { throw "Too many setTimeout calls remain – animation sequencing not fully removed" }
    </automated>
  </verify>
  <done>
    All animation-sequencing setTimeout calls removed from web.ts. CSS animation-duration properties are explicitly declared. When the game runs, animation durations are resolved from CSS computed styles.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify combat animations work correctly in browser</name>
  <files>apps/server/src/routes/web.ts, apps/server/static/css/game.css</files>
  <action>
    Human-verify checkpoint: Start the server, enter a dungeon, and verify all combat animations play correctly through the new pipeline with no regressions.
  </action>
  <verify>
    <automated>cd apps/server && npx tsc --noEmit</automated>
    <human-check>See how-to-verify steps below for detailed gameplay verification</human-check>
  </verify>
  <done>
    Server compiles and starts. All 9 animation behaviors confirmed working in browser. No console errors.
  </done>
  <what-built>
    The complete pipeline refactoring:
    - `computeAnimationSteps(prev, next)` produces AnimationStep[] from combat state diffs
    - `AnimationQueue.playSteps(steps)` processes them via WAAPI promises/FIFO queue
    - `EntityAnimFSM` prevents overlapping animations per entity
    - Animation timing driven by CSS `animation-duration` values
  </what-built>
  <how-to-verify>
    1. Start the server: `cd apps/server && npx tsx src/index.ts`
    2. Open http://localhost:3000/game in a browser
    3. Login and enter a dungeon floor
    4. Observe combat rounds — verify:
       - [ ] Heroes attack with lunge animation (melee) or projectile (mage/range)
       - [ ] Monsters flash white/red on hit
       - [ ] Floating damage numbers appear on hit
       - [ ] Heal animations pulse green on healing
       - [ ] Tank hero shows block animation (shield glow) when blocking
       - [ ] Hero death shows fade-out animation
       - [ ] Monster death shows fade-out animation
       - [ ] No two animations play on the same entity simultaneously (FSM guard)
       - [ ] Animations transition smoothly between rounds (no glitches)
    5. Verify no console errors related to animation in browser DevTools
    6. Verify that if tab is backgrounded, animations still complete (timeout fallback per D-03)
  </how-to-verify>
  <resume-signal>Type "approved" if all animations work correctly, or describe any issues found (e.g., "melee lunge doesn't play", "animations overlap", "console error: ...")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web.ts (client script) → CombatDiff | CombatRoundState from server crosses into diff logic |
| AnimationQueue → DOM | CSS class manipulation on hero/monster card elements |
| EntityAnimFSM → AnimationQueue | FSM state governs which DOM mutations are allowed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Denial of Service | AnimationQueue tab-throttling guard | mitigate | The 2s timeout guard prevents queue deadlock when tab is backgrounded — resolves via force-callback after timeout |
| T-02-05 | Spoofing | EntityAnimFSM entityId resolution | accept | entityId is derived from server state (heroId); spoofed IDs produce no-op |
| T-02-06 | Elevation of Privilege | AnimationQueue step execution | accept | All code runs client-side; no server-side escalation possible |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages added in this phase |
</threat_model>

<verification>
- `npx tsc --noEmit` passes (TypeScript compilation)
- Server starts without errors on `:3000`
- Combat animations play correctly in browser (verified via checkpoint)
- All 17 original setTimeout calls reduced to ≤2 non-animation survivors
- No magic number animation durations remain — all driven by CSS
</verification>

<success_criteria>
1. web.ts compiles without errors
2. Server starts and serves game pages
3. All combat animation types (attack, hit, heal, block, death, monster-death) play correctly through the pipeline
4. No overlapping animations on the same entity — EntityAnimFSM enforces single-state-per-entity
5. No animation-sequencing setTimeout remains (only fallback + non-animation timeouts)
6. getComputedStyle resolves animation-duration from CSS (not hardcoded JS magic numbers)
</success_criteria>

<output>
Create `.planning/phases/02-animation-pipeline/02-02-SUMMARY.md` when done.
</output>
