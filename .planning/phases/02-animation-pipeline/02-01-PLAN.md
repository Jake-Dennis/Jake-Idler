---
phase: 02-animation-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/server/src/animation/CombatDiff.ts
  - apps/server/src/animation/AnimationQueue.ts
  - apps/server/src/animation/EntityAnimFSM.ts
  - apps/server/src/animation/types.ts
autonomous: true
requirements:
  - ANIM-01
  - ANIM-02
  - ANIM-03
user_setup: []

must_haves:
  truths:
    - "Combat state differences can be computed as typed AnimationStep[]"
    - "AnimationQueue processes steps sequentially with WAAPI promises"
    - "Each entity has an independent FSM preventing overlapping animations"
    - "FSM transitions respect IDLE→ATTACKING→IMPACT→RECOVERY→IDLE ordering"
    - "FSM has guards preventing transitions on dead entities"
  artifacts:
    - path: "apps/server/src/animation/types.ts"
      provides: "Shared types (AnimationStep, CombatEffectType, AnimEntityState, FsmTransition)"
      min_lines: 30
    - path: "apps/server/src/animation/CombatDiff.ts"
      provides: "Pure diffing function — prev/next CombatRoundState + PartyHeroRoundData → AnimationStep[]"
      min_lines: 40
    - path: "apps/server/src/animation/AnimationQueue.ts"
      provides: "Promise-based queue consuming AnimationStep[], playing via WAAPI animation.finished / animationend"
      min_lines: 60
    - path: "apps/server/src/animation/EntityAnimFSM.ts"
      provides: "Per-entity FSM with states IDLE/ATTACKING/IMPACT/RECOVERY/DEAD and legal transition map"
      min_lines: 50
  key_links:
    - from: "CombatDiff.ts"
      to: "types.ts"
      via: "imports AnimationStep, CombatEffectType"
      pattern: "import.*AnimationStep|import.*CombatEffectType"
    - from: "AnimationQueue.ts"
      to: "types.ts"
      via: "imports AnimationStep, CombatEffectType, AnimEntityState"
      pattern: "import.*AnimationStep|import.*AnimEntityState"
    - from: "EntityAnimFSM.ts"
      to: "types.ts"
      via: "imports AnimEntityState, FsmTransition"
      pattern: "import.*AnimEntityState|import.*FsmTransition"
    - from: "AnimationQueue.ts"
      to: "EntityAnimFSM.ts"
      via: "calls canTransition/transitionTo before WAAPI playback"
      pattern: "(canTransition|transitionTo)"
---

<objective>
Create the three core animation pipeline modules as leaf-sidecar files — CombatDiff (state diffing), AnimationQueue (WAAPI promise-based sequencing), and EntityAnimFSM (per-entity state machine) — along with shared types.

Purpose: Decouple animation sequencing from combat state updates (ANIM-02), eliminate setTimeout chains in favor of WAAPI promises (ANIM-01), and prevent overlapping animations per entity (ANIM-03).

Output:
- `apps/server/src/animation/types.ts` — shared type definitions
- `apps/server/src/animation/CombatDiff.ts` — pure diff function
- `apps/server/src/animation/AnimationQueue.ts` — promise queue
- `apps/server/src/animation/EntityAnimFSM.ts` — per-entity FSM
</objective>

<execution_context>
@C:/Users/JakeP/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/JakeP/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

The server compiles via tsc inside a Turborepo. Client-side JS currently lives in web.ts as a concatenated `<script>` block. All three new modules are server-side TypeScript that will be bundled in the tsc compile. They export factory/class functions consumed by the client-side code in web.ts.

Existing animation classes in game.css (lines 117-134): `.animate-lunge`, `.animate-flash-white`, `.animate-flash-red`, `.animate-pulse-green`, `.animate-shake`, `.animate-fade-out`, `.animate-shield`, `.animate-explode`.

Combat state types in `apps/server/src/services/combat-service.ts`: `CombatRoundState` and `PartyHeroRoundData` drive the diff logic.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared types and CombatDiff pure function</name>
  <files>apps/server/src/animation/types.ts, apps/server/src/animation/CombatDiff.ts</files>
  <behavior>
    - Test 1: CombatDiff returns empty AnimationStep[] when prev and next round are identical
    - Test 2: CombatDiff produces an ATTACK step when a party hero dealt damage in the round
    - Test 3: CombatDiff produces a HIT step when a party hero took damage
    - Test 4: CombatDiff produces a HEAL step when a party hero received healing
    - Test 5: CombatDiff produces a BLOCK step when a tank hero blocked damage
    - Test 6: CombatDiff produces a DEATH step when a hero died (alive→!alive transition)
    - Test 7: CombatDiff produces a MONSTER_KILL step when monsterJustKilled toggles
    - Test 8: CombatDiff produces steps ordered by round-logic sequence (attacks before heals before deaths)
  </behavior>
  <action>
    Create `apps/server/src/animation/types.ts` with these exports:

    - `CombatEffectType` union: "ATTACK" | "HIT" | "HEAL" | "BLOCK" | "DEATH" | "MONSTER_DEATH" | "MONSTER_APPEAR"
    - `AnimationStep` interface:
      - `type: CombatEffectType`
      - `entityId: string | null` (null for monster-global effects)
      - `weaponType?: "melee" | "mage" | "range"`
      - `damage?: number`
      - `healAmount?: number`
      - `isCrit?: boolean`
      - `cssDurationMs?: number` (populated externally by AnimationQueue via getComputedStyle)
      - `stepIndex: number` (sequential ordering within round)
    - `AnimEntityState` union: "IDLE" | "ATTACKING" | "IMPACT" | "RECOVERY" | "DEAD"
    - `FsmTransition` interface: { from: AnimEntityState; to: AnimEntityState }
    - `FSM_STATE_ORDER` const array for ordering

    Create `apps/server/src/animation/CombatDiff.ts`:

    - Export a pure function `function computeAnimationSteps(prev: CombatRoundState | null, next: CombatRoundState): AnimationStep[]`
    - Early return `[]` when prev is null (first poll, no diff)
    - Check `next.round.round <= (prev?.round?.round ?? -1)` — skip duplicate rounds
    - Iterate `next.round.partyHeroes` and compare each to its counterpart in `prev.round.partyHeroes` (matched by heroId):
      - If hero dealt damage → push ATTACK step with weaponType and isCrit, and an IMPACT HIT step on the monster target
      - If hero took damage AND role is tank → push HIT + BLOCK steps
      - If hero took damage AND role is not tank → push HIT step
      - If hero received healing → push HEAL step
      - If hero was alive in prev and !alive in next → push DEATH step
    - Check `next.round.monsterJustKilled && !prev.round.monsterJustKilled` → push MONSTER_DEATH step
    - Check monster name change → push MONSTER_APPEAR step
    - Assign sequential `stepIndex` to each produced step to preserve order

    Import types from `./types.js` (ESM extension for tsc resolution). Use `import type` for interface-only imports.

    Per D-01: Pure data flow — no DOM access, no side effects.
    Per D-02: No external animation libraries.
  </action>
  <verify>
    <automated>npx jest --testPathPattern="CombatDiff" --no-coverage 2>/dev/null || echo "MISSING — Wave 0 must create apps/server/src/animation/CombatDiff.test.ts first"</automated>
  </verify>
  <done>
    CombatDiff.test.ts exists with tests for all 8 behaviors. All tests pass. CombatDiff.ts exports computeAnimationSteps as a pure function. types.ts exports all interfaces.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Promise-based AnimationQueue with WAAPI-driven sequencing</name>
  <files>apps/server/src/animation/AnimationQueue.ts</files>
  <behavior>
    - Test 1: AnimationQueue.enqueue() stores a step and returns a promise
    - Test 2: AnimationQueue steps play in FIFO order (mock WAAPI animation.finished)
    - Test 3: AnimationQueue processes all steps before resolving the queue promise
    - Test 4: AnimationQueue reads animation-duration from getComputedStyle (mock) for CSS timing
    - Test 5: AnimationQueue falls back to a default duration (300ms) when element or computed style is missing
    - Test 6: AnimationQueue handles an empty step array gracefully (resolves immediately)
    - Test 7: AnimationQueue stops processing when it encounters a DEAD entity (guard via FSM)
  </behavior>
  <action>
    Create `apps/server/src/animation/AnimationQueue.ts`:

    - Export class `AnimationQueue` with constructor taking:
      - `fsm: EntityAnimFSM` (reference to the FSM instance)
      - `getAnimDuration?: (className: string) => number` (injectable duration lookup; default reads `animation-duration` via getComputedStyle)
    
    - Method `async playSteps(steps: AnimationStep[]): Promise<void>`:
      - Iterate steps sequentially (FIFO)
      - For each step: check FSM.canTransition(entityId, targetState). If transition not allowed → skip step (entity busy/dead)
      - Call FSM.transitionTo(entityId, targetState) to lock before playing
      - Apply CSS class to target element (via document.getElementById):
        - ATTACK: `.animate-lunge` + `.animate-windup`
        - HIT: `.animate-flash-red` + `.animate-shake`
        - HEAL: `.animate-pulse-green`
        - BLOCK: `.animate-shield`
        - DEATH: `.animate-fade-out`
        - MONSTER_DEATH: `.animate-fade-out` on the focus monster card
        - MONSTER_APPEAR: `.animate-fade-in`
      - Play via WAAPI: create an `Animation` instance via `element.animate()` or attach class and await `animationend` via Promise wrapper:
        - `new Promise(resolve => { el.addEventListener('animationend', resolve, { once: true }); })`
      - On resolution: remove CSS class, call FSM.transitionTo(entityId, "RECOVERY" | "IDLE")
    
    - Private helper `resolveAnimationDuration(className: string): number`:
      - Create a temporary off-screen element, apply className, read `getComputedStyle(el).animationDuration`
      - Parse from `.?s` format to milliseconds, cache result per className
      - Return cached duration or 300ms fallback per D-04 (CSS-driven timing)
    
    - Internal method `ensureEntityId(step: AnimationStep): string`:
      - For entityId === null (monster effects), resolve to `.monster-card.is-focus` or first `.monster-card`

    - Add a 2-second timeout guard per step: if animationend doesn't fire within computed duration + 500ms (tab throttling), force-resolve per D-03 (setTimeout fallback guard only)

    Import from `./types.js` and `./EntityAnimFSM.js`.

    Per D-03: Replace all setTimeout chains with WAAPI animation.finished promises + async/await.
    Per D-04: Read animation durations from CSS via getComputedStyle. No hardcoded magic numbers.
  </action>
  <verify>
    <automated>npx jest --testPathPattern="AnimationQueue" --no-coverage 2>/dev/null || echo "MISSING — Wave 0 must create apps/server/src/animation/AnimationQueue.test.ts first"</automated>
  </verify>
  <done>
    AnimationQueue.test.ts exists and passes. AnimationQueue exports the playSteps async method. Class support is ready for integration into web.ts.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create per-entity animation FSM</name>
  <files>apps/server/src/animation/EntityAnimFSM.ts</files>
  <behavior>
    - Test 1: FSM starts each new entity in IDLE state
    - Test 2: FSM allows IDLE → ATTACKING transition (legal)
    - Test 3: FSM rejects ATTACKING → ATTACKING (no overlap — illegal)
    - Test 4: FSM allows ATTACKING → IMPACT (legal)
    - Test 5: FSM allows IMPACT → RECOVERY (legal)
    - Test 6: FSM allows RECOVERY → IDLE (legal)
    - Test 7: FSM allows any state → DEAD (always legal)
    - Test 8: FSM rejects ATTACKING → IDLE (skipping IMPACT/RECOVERY — illegal)
    - Test 9: FSM prevents any transition on a DEAD entity (no animations on dead)
    - Test 10: FSM.getState(entityId) returns the current state
  </behavior>
  <action>
    Create `apps/server/src/animation/EntityAnimFSM.ts`:

    - Export class `EntityAnimFSM` with:
      - Internal `Map<string, AnimEntityState>` storing per-entityId state
      - `LEGAL_TRANSITIONS` static map defining allowed transitions:
        - IDLE → ATTACKING
        - ATTACKING → IMPACT (auto-transition via animationend)
        - ATTACKING → DEAD (interrupt death)
        - IMPACT → RECOVERY
        - IMPACT → DEAD (interrupt death)
        - RECOVERY → IDLE
        - RECOVERY → DEAD (interrupt death)
        - DEAD → IDLE (respawn/reset)
        - Any → DEAD (always allowed for death interrupt)
    
    - Method `registerEntity(entityId: string): void` — initializes entity to IDLE (no-op if already registered)
    - Method `getState(entityId: string): AnimEntityState` — returns current state, defaults to IDLE for unknown entities
    - Method `canTransition(entityId: string, targetState: AnimEntityState): boolean`:
      - If current state is DEAD → only DEAD → IDLE allowed (guards replay on dead per D-05)
      - Check LEGAL_TRANSITIONS for current→target pair
      - Returns boolean (no side effects)
    - Method `transitionTo(entityId: string, targetState: AnimEntityState): boolean`:
      - Calls canTransition first
      - If allowed: updates map, returns true
      - If not allowed: returns false, no state change
    - Method `forceReset(entityId: string): void` — sets state to IDLE (for cleanup/retry)
    - Method `isDead(entityId: string): boolean` — convenience check

    Per D-05: Each entity (hero/monster) maintains independent state. Guards prevent playing animations on dead/dying entities.
  </action>
  <verify>
    <automated>npx jest --testPathPattern="EntityAnimFSM" --no-coverage 2>/dev/null || echo "MISSING — Wave 0 must create apps/server/src/animation/EntityAnimFSM.test.ts first"</automated>
  </verify>
  <done>
    EntityAnimFSM.test.ts exists and passes all 10 behaviors. EntityAnimFSM exports the FSM class with state tracking and transition guards.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CombatDiff → AnimationQueue | Step data crosses from pure logic to DOM-mutating playback |
| EntityAnimFSM → AnimationQueue | FSM state governs which DOM mutations are allowed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | AnimationQueue CSS class application | mitigate | Entity IDs are server-generated (heroId/uuid); no user-controlled data becomes CSS class names |
| T-02-02 | Denial of Service | AnimationQueue enqueue loop | mitigate | AnimationQueue caps queue at 100 steps max; older steps are dropped with console.warn |
| T-02-03 | Spoofing | EntityAnimFSM entity registration | accept | Entity IDs come from server state; a malformed ID defaults to IDLE with no side effects |
</threat_model>

<verification>
- All 3 module files exist under apps/server/src/animation/
- npm run build compiles without errors
- All 24+ unit tests pass (8 + 7 + 10)
- No commonjs require usage — all imports use ESM
- No setTimeout in CombatDiff.ts or EntityAnimFSM.ts (AnimationQueue.ts may use it only as a 2s tab-throttling fallback guard per D-03)
</verification>

<success_criteria>
1. All three module files compile successfully
2. Test suites pass for CombatDiff, AnimationQueue, and EntityAnimFSM
3. Types are self-documenting with JSDoc comments on all exported interfaces
4. No external animation libraries introduced (confirm package.json unchanged)
</success_criteria>

<output>
Create `.planning/phases/02-animation-pipeline/02-01-SUMMARY.md` when done.
</output>
